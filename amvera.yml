# Hosting settings
meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  artifacts:
    "backend": /
  additionalCommands: npm run build

run:
  command: npm run start
  containerPort: 5000
